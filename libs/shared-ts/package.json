{
  "name": "@auctiondeal/shared-ts",
  "version": "1.0.0",
  "description": "Shared TypeScript utilities for Auctiondeal services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "prisma:generate": "prisma generate",
    "prisma:db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "winston": "^3.11.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.22.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist"
  ]
}
